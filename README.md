### winutils - handpicked favorites from coreutils as their windows equivalent
#### Use the provided powershell profile file (Microsoft.PowerShell_profile.ps1) to enable everything I have gathered so far
Save it as a file in the $PROFILE variable's path (default variable in powershell; it is used to read a user script before loading a powershell terminal).
Other files are just pieces of my PS profile.
